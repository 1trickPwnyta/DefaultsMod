using Verse;

namespace Defaults.AllowedAreas
{
    public class CompProperties_SpawnTracker : CompProperties
    {
        public CompProperties_SpawnTracker()
        {
            compClass = typeof(Comp_SpawnTracker);
        }
    }
}
